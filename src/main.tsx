import React from "react";
import ReactDOM from "react-dom/client";

// Placeholder shell — replaced by the observer UI (src/ui/).
ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <div style={{ fontFamily: "serif", padding: 40 }}>
      <h1>Aeonspire</h1>
      <p>The observer UI is being assembled…</p>
    </div>
  </React.StrictMode>,
);
